# Missing Receipt does not block payout

Status: accepted

A Claim may be verified and paid without an attached Receipt. The UI must show a persistent `Missing Receipt` warning and recommend attaching at least one image, but the condition is not a workflow blocker.
