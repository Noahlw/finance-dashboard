# Receipt totals cap split Claims when available

Status: accepted

When one Receipt is reused across multiple Claims, the combined Claim amounts linked to it must not exceed the Receipt total when that optional metadata exists. If no Receipt total is recorded, intake remains allowed but the split is flagged for review rather than blocked.
