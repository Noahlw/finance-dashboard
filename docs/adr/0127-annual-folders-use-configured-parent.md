# Annual folders use a configured Drive parent

Status: accepted

Setup stores the configured `CF Budget` parent Drive folder and creates each academic-year folder beneath it. Migration must not depend on the Drive root or require the Treasurer to choose a parent on every run.
