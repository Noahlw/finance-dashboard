# Web access is limited to committee operators

Status: accepted

Only `COMMITTEE` and `TREASURER` users may authenticate to and operate the web app. `MEMBER` users are represented as Claimants rather than web users, and `ADVISOR_AUDITOR` access is out of scope for v1. The boundary reflects the WhatsApp intake process and limits finance operations to the roles responsible for them.
