# Annual rollover uses an interactive Treasurer migration wizard

Status: accepted

When a new academic-year spreadsheet is created, the system will guide the Treasurer through an interactive migration wizard. The Treasurer can select which existing members to carry forward, add new members, leave departed members out, review Finance Accounts, and enter each account’s opening balance before activating the new annual file. The wizard must preserve selected identity data without requiring a full re-entry.
