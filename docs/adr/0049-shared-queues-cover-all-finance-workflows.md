# Shared queues cover all finance workflows

Status: accepted

The Dashboard will use shared queues for Claims, Budget Requests, Payouts, and related finance tasks. Both `COMMITTEE` and `TREASURER` operators may view the queues; role-specific actions remain enforced, including Treasurer-only payout actions.
