# Committee work uses shared queues

Status: accepted

The Dashboard will expose shared queues to all authorized `COMMITTEE` and `TREASURER` operators. Operators can see and filter all Claims, regardless of creator, for review, handover, and follow-up; self-verification and other role rules remain enforced server-side.
