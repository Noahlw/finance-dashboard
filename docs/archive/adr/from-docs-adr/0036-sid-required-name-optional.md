# SID is required and Claimant name is optional

Status: accepted

Every Claim must include the complete manually entered SID for its Claimant. Claimant name is optional and may contain a real name or nickname. SID is an identity field only, not an authentication credential; a future attendance integration may validate and populate the name.
