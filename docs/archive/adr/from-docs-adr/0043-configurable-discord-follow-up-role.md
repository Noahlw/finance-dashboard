# Discord follow-up role is spreadsheet-configured

Status: accepted

The Discord role responsible for Claim information follow-up will be configured by role ID in the spreadsheet configuration rather than hard-coded in source. `NEEDS_INFO` notices will mention that role in the existing configured Discord channel, allowing responsibility to change through role membership.
