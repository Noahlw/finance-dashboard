# Income supports Treasurer review and correction

Status: accepted

Treasurer may move a pending Income entry to `NEEDS_INFO` or reject it with a reason. Committee Operators may correct a `NEEDS_INFO` entry and resubmit it to `PENDING`; each decision and correction is audited. Only approved Income contributes to balances and official reports.
