# Only approved Budget Requests fund Claims

Status: accepted

New Claims may select Budget Lines only when their parent Budget Request is approved. Submitted, pending, `NEEDS_INFO`, rejected, and closed requests cannot fund new Claims.
