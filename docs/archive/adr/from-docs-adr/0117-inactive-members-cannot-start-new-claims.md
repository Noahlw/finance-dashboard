# Inactive migrated members cannot start new Claims

Status: accepted

When a member is marked inactive during Annual Migration, the new annual file preserves the member for historical lookup but excludes them from new Claimant selection. The Treasurer may manually reactivate the member later if a new Claim is legitimately required.
