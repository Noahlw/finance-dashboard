# Payout reference is required for electronic methods

Status: accepted

When marking a Payout as `SENT`, the Treasurer must provide a transaction reference for `FPS` or `PAYME`. The reference is optional for `OTHER` payment methods.
