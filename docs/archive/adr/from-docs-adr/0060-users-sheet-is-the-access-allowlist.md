# The Users sheet is the access allowlist

Status: accepted

The production web app will allow signed-in Google accounts only when their email appears in an active `Users` row with an allowed role. `citycf41@gmail.com` is the recommended primary Treasurer account, but the app must also support other explicitly provisioned accounts; the recommendation is not an authentication restriction.
