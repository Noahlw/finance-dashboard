# Claim mutability follows the review lifecycle

Status: accepted

Committee users may edit Claim details and Receipts while a Claim is `SUBMITTED` or `NEEDS_INFO`. Once it is `VERIFIED`, normal editing stops; corrections require an explicit authorized reopen path and an audit record. `APPROVED_FOR_PAYOUT`, `PAID`, `LOCKED`, and `REJECTED` Claims are immutable outside that controlled process.
