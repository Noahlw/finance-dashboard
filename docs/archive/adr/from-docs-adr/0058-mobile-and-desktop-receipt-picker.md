# Receipt intake supports mobile and desktop file selection

Status: accepted

The Receipt input will use one responsive picker that supports mobile camera photos, mobile gallery/downloaded files, desktop image files, and PDFs. It will retain the 5 MB per-file limit and multiple-file support; camera capture is a convenience, not a requirement.
