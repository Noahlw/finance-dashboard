# Semester dates are Treasurer-configured

Status: accepted

Each annual file stores the start and end dates for `SEM A`, `SEM B`, and `SUMMER` in spreadsheet Config. The Treasurer maintains these ranges instead of relying on hard-coded calendar dates.
