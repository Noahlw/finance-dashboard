# Dashboard uses swipeable mobile views

Status: accepted

The mobile-first Dashboard will provide several horizontally swipeable views of the current finance workspace. Desktop browsers will expose the same views through equivalent tabs or controls. The views are operational presentations backed by the spreadsheet, not a requirement to expose raw storage tables directly.
