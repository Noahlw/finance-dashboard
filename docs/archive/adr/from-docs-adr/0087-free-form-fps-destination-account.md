# FPS destination accounts are free-form

Status: accepted

The FPS destination-account field accepts free-form text with basic trimming and length validation rather than a bank-specific numeric format. The Treasurer verifies the saved destination before sending the Payout.
