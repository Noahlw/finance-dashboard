# Production releases require explicit approval

Status: accepted

CI will automatically run tests, lint, and the frontend build, but production deployment requires an explicit release action or approval. Ordinary merges must not deploy finance-system changes automatically.
