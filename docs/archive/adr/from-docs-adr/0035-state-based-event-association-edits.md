# Event association follows Claim mutability

Status: accepted

Committee Operators may add, change, or remove a Claim’s Event link while the Claim is `SUBMITTED` or `NEEDS_INFO`. After `VERIFIED`, only the Treasurer may change the link with an audit reason; `PAID` and `LOCKED` Claims cannot change Event association.
