# Drafts save at explicit workflow boundaries

Status: accepted

The mobile Claim stepper saves a Draft when the operator taps `Save Draft` or advances between steps. It does not persist every keystroke, limiting writes while still protecting progress during normal interruptions.
