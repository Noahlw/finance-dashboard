# Cross-operator Receipt duplicates require review

Status: accepted

If a Committee Operator uploads a Receipt whose content hash already exists under another operator, submission is blocked and the duplicate is flagged for Treasurer review. Same-operator reuse remains allowed for legitimate Claim splitting.
