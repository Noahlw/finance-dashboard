# Legacy payout methods are abandoned

Status: accepted

The new Claim payment model supports only `FPS`, `PAYME`, and `OTHER`. The previous `BANK` and `CASH` methods are abandoned and must not remain selectable or supported by new application flows. Historical records may require a separate migration/display strategy, but the old methods are no longer part of the active domain model.
